GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=global
GEMINI_MODEL=gemini-2.5-flash

# Optional: enables YouTube Data API metadata fetching.
# Without this, the app falls back to YouTube oEmbed.
YOUTUBE_API_KEY=
